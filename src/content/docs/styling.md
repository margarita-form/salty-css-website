---
title: Styling
description: Author component styles, design tokens, themes, fonts, variants, media queries, animations, and templates with Salty CSS.
preHeadline:
  react: Typed styled() Components, defineVariables Tokens, and Theming Without a React Provider
  next: Author Component Styles Inside .css.ts Files That Compile Once, Stream as Plain CSS Through RSC, and Skip the Runtime Bundle
  astro: Write Component Styles in .css.ts and Compile Them Alongside .astro Pages With Zero Client JavaScript
visibleHeading:
  react: Styling Salty CSS Components in React
  next: Styling Salty CSS Components in Next.js
  astro: Styling Salty CSS Components in Astro
topic: Styling
category: guide
schemaType: TechArticle
keywords: [styling, styled, classnames, variables, tokens, theming, fonts, variants, overrides, media queries, animations, templates]
intent: Index every styling-side doc so authors can find the right primitive — styled, className, tokens, theming, fonts, variants, overrides, media queries, animations, templates.
proficiencyLevel: Beginner
priority: 0.7
previousPage: none
nextPage: basics
---

The styling section walks through every primitive Salty CSS gives you for authoring real CSS from TypeScript — from the basic `styled()` component and `className()` helpers, to design tokens, theming, font registration, variants, overrides, media queries, animations, and reusable templates. Start with **Component styles** and pick up the others as you need them.
